APP_PORT=8080

DB_HOST=localhost
DB_PORT=5432
DB_NAME=auth_db
DB_USER=postgres
DB_PASSWORD=postgres

JWT_SECRET=change-me-generate-a-long-random-value-e.g-openssl-rand-hex-32
# Must match JWT_SECRET in master_service (and any other service that verifies these tokens) exactly

JWT_ACCESS_EXP=15m